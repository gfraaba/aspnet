{
    "name": "ASP.NET Dev Container",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
            "PLATFORM": "${localEnv:PLATFORM:linux/arm64}",
            "DOTNET_SDK_VERSION": "6.0",
            "PWSH_VERSION": "7.4.6"
        }
    },
    "customizations": {
      "vscode": {
        "extensions": [
          "ms-dotnettools.csharp",
          "ms-dotnettools.csdevkit"
        ],
        "settings": {
          "terminal.integrated.shell.linux": "/usr/bin/pwsh"
        }
      }
    },
    "forwardPorts": [5000, 5001],
    "mounts": [ "source=/Users/schada/github/certs,target=/root/.aspnet/https,type=bind" ],
    "postCreateCommand": "dotnet --info && cd SampleAspNetApp && dotnet restore",
    "runArgs": ["--platform", "linux/arm64"]
  }
  